# 1. API Rewrite Rules (Must come first so they are not caught by the wildcard SPA rule)
# If you deploy your backend Express server to a platform like Render or Railway,
# uncomment the line below and replace it with your actual backend server URL:
# /api/*  https://your-salonsync-backend.onrender.com/api/:splat  200

# 2. Client-Side Routing Fallback (Wildcard SPA routing rule)
# Redirects all sub-paths to index.html for React Router client-side routing on page refresh
/*    /index.html   200
